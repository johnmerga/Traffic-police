import 'package:flutter/material.dart';

// ignore: camel_case_types
class Acc_Reports extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Accident Reports")),
    );
  }
}
