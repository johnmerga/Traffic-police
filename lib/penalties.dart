import 'package:flutter/material.dart';

class Penalties extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Penalties list")),
    );
  }
}
