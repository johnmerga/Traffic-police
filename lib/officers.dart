import 'package:flutter/material.dart';

class Officers extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Officers list")),
    );
  }
}
